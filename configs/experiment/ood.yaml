# @package _global_

# to execute this experiment run:
# python main.py +experiment=ood

defaults:
  - override /quantization: static_quantization.yaml
  - override /ood: svhn_datamodule.yaml

exp_id: null # id of model
exp_name: null # optional mlflow experiment name
compressed_path: null # optional path to pruned model

trainer:
  gpus: 0

# gpu_id: "3"

logger:
  mlflow:
    tags:
      type: pruning + quantization + ood
      # type: pruning + ood

test:
  ood: true
