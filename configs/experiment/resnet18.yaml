# @package _global_

# to execute this experiment run:
# python main.py +experiment=resnet18
defaults:
  - override /model: resnet18.yaml
  - override /datamodule: cifar_datamodule.yaml

seed: 42

trainer:
  max_epochs: null

datamodule:
  batch_size: 128

logger:
  mlflow:
    experiment_name: "CIFAR_RESNET18"
