# @package _global_

# to execute this experiment run:
# python main.py +experiment=mobilenetv2
defaults:
  - override /model: mobilenetv2.yaml
  - override /datamodule: cifar_datamodule.yaml

seed: 42

trainer:
  max_epochs: null

logger:
  mlflow:
    experiment_name: "CIFAR_MOBILEV2"
