# @package _global_

# to execute this experiment run:
# python main.py +experiment=evaluation

exp_id: null # id of model

gpu_id: "3"

logger:
  mlflow:
    tags:
      type: eval

test:
  corruptions:
    severities:
      - 1
      - 2
    corruptions:
      - motion_blur
      - gaussian_blur
    folder_name: CIFAR10
    dataset_path: null
