# @package _global_

defaults:
  - override /quantization: static_quantization.yaml

exp_id: null # id of model
exp_name: null # optional mlflow experiment name
compressed_path: null # optional path to pruned model

trainer:
  gpus: 0

logger:
  mlflow:
    tags:
      type: quantization

test:
  corruptions:
    severities:
      - 1
      - 2
    corruptions:
      - motion_blur
      - gaussian_blur
    folder_name: CIFAR10
    dataset_path: null
