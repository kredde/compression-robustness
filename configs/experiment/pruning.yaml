# @package _global_

defaults:
  - override /pruning: unstructured_l1.yaml

exp_id: "99c9ed56ad094470a2f759be807495af"

exp_name: null

gpu_id: "3"

logger:
  mlflow:
    tags:
      type: pruning
      pruning_method: "unstructured_l1"

test:
  corruptions:
    severities:
      - 1
      - 2
    corruptions:
      - motion_blur
      # - jpeg_compression
      # - contrast
      # - brightness
      - gaussian_blur
    folder_name: CIFAR10
    dataset_path: null
