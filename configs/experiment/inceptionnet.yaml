# @package _global_

# to execute this experiment run:
# python main.py +experiment=inceptionnet
defaults:
  - override /model: inceptionnet.yaml
  - override /datamodule: cifar_datamodule.yaml

seed: 42

trainer:
  max_epochs: null

logger:
  mlflow:
    experiment_name: "CIFAR_INCEPTION"
